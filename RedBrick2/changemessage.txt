
1) The "Create Cutlist" window now resolves lightweight components before trying to traverse the tree.
2) Un-crisscrossed edging oversize calculation.
3) Enter and backspace act just the way you'd expect in the Finish fields on drawings.
4) Closed a loophole that allowed Chris' naughty characters to get in the Cutlist Database.
5) Fixed a label that wasn't hiding when it should.
6) [Tab] will work like [ENTER] in the "Material by M2M number" popup.
